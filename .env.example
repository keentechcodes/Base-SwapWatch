# Server Configuration
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Coinbase Developer Platform
CDP_API_KEY_NAME=your-api-key-name-here
CDP_API_KEY_PRIVATE_KEY=your-api-private-key-here
WEBHOOK_SECRET=your-webhook-secret-here

# Webhook Configuration
WEBHOOK_ENDPOINT=/webhook
NETWORK_ID=base-mainnet

# Wallet Addresses to Monitor (comma-separated)
MONITORED_WALLETS=0x123...,0x456...

# ============================================
# API DATA ENRICHMENT CONFIGURATION
# ============================================

# DexScreener API (No key required!)
# Public API for real-time token prices and market data
DEXSCREENER_API_URL=https://api.dexscreener.com/latest

# Etherscan API v2 (Required for contract verification on Base chain)
# Sign up at: https://etherscan.io/apis
# This single API key works for all 50+ supported chains including Base
# Free tier: 100,000 calls/day, 5 calls/second
ETHERSCAN_API_KEY=your_etherscan_api_key_here
# Note: Service automatically uses chainid=8453 for Base

# Token Metadata RPC Endpoint (Choose one option)
# Option 1: Public RPC (no key needed, may be rate limited)
BASE_RPC_URL=https://mainnet.base.org

# Option 2: Alchemy (better performance, requires free account)
# Sign up at: https://www.alchemy.com/
# ALCHEMY_API_KEY=your_alchemy_key_here
# BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/your_alchemy_key_here

# Option 3: Moralis (additional features, requires account)
# Sign up at: https://moralis.io/
# MORALIS_API_KEY=your_moralis_key_here

# Redis Cache Configuration
# Local development: no password needed
# Production: use Redis Cloud (https://redis.com/try-free/)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Cache TTL Settings (in seconds)
CACHE_TTL_MARKET=300          # 5 minutes for price data
CACHE_TTL_METADATA=7200       # 2 hours for token names/symbols
CACHE_TTL_VERIFICATION=86400  # 24 hours for contract verification
CACHE_TTL_FACTORY=86400       # 24 hours for factory/deployer info

# API Rate Limiting Configuration
ETHERSCAN_RATE_LIMIT=5        # Max requests per second
API_RETRY_ATTEMPTS=3          # Number of retry attempts
API_RETRY_DELAY=1000          # Initial retry delay in milliseconds

# Optional: Admin API Key for cache management
# Generate a secure random string for production
ADMIN_API_KEY=your_secure_admin_key_here

# ============================================
# OPTIONAL SERVICES
# ============================================

# Ngrok Configuration (for local development)
NGROK_AUTHTOKEN=your-ngrok-auth-token-here